<div class='text-center'>
  <h2>{{comment.title}}</h2>
  <p>{{comment.content}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <button class='btn btn-primary'>Reply to this Comment!</button>
  </p>
</div>